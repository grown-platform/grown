# Org Sync

Directory synchronization between the workspace and external identity sources.

## Desktop

![Org Sync — desktop](screenshots/orgsync-desktop.png)

---

_Live-captured at `http://workspace.localtest.me:8080/orgsync` against the local full stack, authenticated as `admin@grown.localtest.me` via Zitadel SSO._
