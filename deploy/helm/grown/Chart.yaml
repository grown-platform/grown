apiVersion: v2
name: grown
description: >-
  All-in-one Grown Workspace platform — bundles Postgres, MinIO (S3), Zitadel
  (OIDC) and the grown app (with optional bolo-mp sidecar). No external
  operators required; runs on kind, a Raspberry Pi 5 cluster, or a homelab.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Default grown app version installed (overridable via image.tag).
appVersion: "20260613-150615-2d752d1c"
keywords:
  - grown
  - workspace
  - collaboration
  - oidc
  - zitadel
home: https://github.com/yieldllc/grown
maintainers:
  - name: Grown Workspace
sources:
  - https://github.com/yieldllc/grown
